# 翻滚与犀王难度升级

## 操作

- 步行仍为基础 2 倍（6 米/秒）；跑步从 4 倍降到 **3 倍（9 米/秒）**，Shift 切换。
- **WASD / 方向键 + 空格**：向输入方向翻滚，无输入时向角色面朝方向翻滚。手机有独立「翻滚」按钮。
- 每次翻滚 0.48 秒、最远 4.1 米，2.2 秒冷却。0.025–0.32 秒为短暂无敌，脚下圈变为青色；右下按钮显示冷却数值与恢复进度。
- 使用真实小步碰撞，不穿越墙、树木、河岸或 Boss 身体。动画收身、前滚、起身；减少动态时为矮身闪避，不作整圈旋转。
- 翻滚期间不能攻击/拾取/换装。翻滚可取消尚未命中的挥剑、挥镐或钓鱼；已钓获的收鱼动画和船上不能翻滚。面板暂停翻滚和冷却，按住空格不会自动连续翻滚。
- **F** 取代以前空格的工具/攻击/挖矿/钓鱼操作，鼠标、E 和触屏原操作保留。界面提示、帮助和验证脚本已同步。

## 犀王强化

- 生命 40 → **72**，追击 3.3 → **7.2 米/秒**，冲锋 15 → **21 米/秒**。
- 普通冲锋预警 0.82 秒，宽 3 米、最长 11.4 米，每次命中扣 **2 格体力**；方向仍在预警开始后锁定。
- 新增近身抬蹄震地：0.62 秒圆形预警，半径 3.25 米、1 格伤害、0.85 秒收势，可走出或时机正确地翻滚躲避。
- 半血进入狂怒：速度 25 米/秒、双段冲锋，两段各自重新预警；第二冲后有 1.15 秒反击窗口。不会无提示掉头追踪。
- 仅玩家仇恨、完整沙滩边界、障碍裁剪、回家恢复、失败不丢物品和一次性击败奖励保持不变。

## 主要实现

- `components/game/dodge.ts`：方向、位移积分、冷却、无敌窗口与姿势曲线。
- `components/game/character.ts`：独立翻滚枢轴、收身动作、实际模型底部贴地和工具姿势还原。
- `components/game/DodgeButton.tsx` / `dodge.css`：桌面/触屏翻滚与冷却 UI。
- `components/game/beachBoss.ts` / `beachBossView.ts` / `BossHud.tsx`：震地、狂怒连冲、精确预警和反馈。
- `components/game/engine.ts`：空格/F 映射、交互互斥、翻滚免伤、暂停和复位。

## 验证

- `npm run test:dodge`：速度、归一化方向/面朝方向、距离/冷却、不可连滚、墙体、所有朝向实际翻滚模型不穿地、减少动态、真实 Boss 冲锋翻滚免伤。
- `npm run test:beach-boss`：20 项模型/真实世界验证及视觉/音频检查，新增震地和半血双冲，保留边缘引导及安全返回检查。
- `npm run test:dodge:browser`：真实空格翻滚、冷却/暂停、防长按重复、F 挥剑和翻滚打断、实际农场围栏、手机按钮布局/触屏和减少动态。
- `npm run test:beach-boss:browser`：现有预览实战，21 次冲锋预警、15 次翻滚，经历狂怒第二段冲锋并击败；一次性奖励和声音验证，无运行时异常。
- `test:fishing:browser`、`test:mining:browser`、`test:sword:browser` 已验证新 F 键绑定，鼠标与手机旧操作不受影响。
- 世界/长剑回归、类型检查、修改模块专项 lint、生产构建、`git diff --check` 通过。既有构建大 chunk 提示保留；没有声称模板通用测试或全项目 lint 全部通过。

所有浏览器检查使用已有 `http://127.0.0.1:5177`，没有启动新的游戏服务器；截图在忽略目录 `.cache/`。
