# 长剑持握与挥斩优化

- 默认是向前、向外、斜向上的持剑姿势：世界中约 60°，初始固定视角下呈清楚的约 45° 斜线，走路和奔跑时仍保持剑尖抬起。
- 剑柄改为位于掌心，独立腕部枢轴使剑随手腕转动，不再从肘关节绕飞；增加剑柄缠带、剑脊和渐尖剑尖。
- 左键挥剑，或按 F 向面前出剑；空格现为翻滚，可以取消尚未命中的挥剑。
- 一次动作 0.60 秒：引剑到右肩 → 转肩伸肘快速斩击 → 向左前方随势挥过 → 缓缓收回同一持握姿势。身体和副手配合发力，脚部小幅撑开后自然恢复。
- 关键帧采用连续速度插值，剑刃经过命中点时不会突然停住；剑光只在切割阶段出现，直接采样真实剑刃路径，而非脚下的固定圆弧。
- 0.13 秒出剑时播放破风声，0.22 秒到达命中阶段才结算原有的 2 点伤害，每次仅一次；方向在起手时锁定。间隔 0.70 秒避免收势未完成就重新起手。
- 切换武器、丢下剑、打开面板、失焦或被 Boss 撞中会取消未完成的出剑；不会隔空补发命中。减少动态关闭残影、减小装饰性身体晃动，保留可辨认的持握与斩击。
- 镰刀、矿镐、钓竿、手枪和乘船姿势保持独立，所有新增手腕/上身变换每帧清零后按当前工具应用。

主要入口：`components/game/swordMotion.ts`、`character.ts`、`farmView.ts`、`swordTrail.ts`、`engine.ts`。

```text
npm run test:sword
npm run test:sword:browser
npm run test:mining
npm run test:world
npm run test:beach-boss
npm run typecheck
npm run build
```

浏览器验证使用现有 `http://127.0.0.1:5177`，不启动游戏服务器；截图保存在忽略目录 `.cache/`。

## 验证结果

- 实际骨架量测：默认剑根 y=0.979、剑尖 y=1.626；剑刃向外并向前上方倾斜，初始画面投影向右 0.344、向上 0.315，不再因透视方向几乎缩成一点。
- 几何 / 时序专项测试通过：走跑持剑抬起、全程不穿地、掌心固定、连续速度、前方接触点与随势、动作复位、其他工具/乘船互不污染、真实残影轨迹、一次性延迟命中和中断取消。
- 真实 Chromium 通过：待机/走跑、蓄力/斩击/收势、破风时机、防连点重启、切枪/面板取消、手机触屏与减少动态，无运行时异常。
- 挖矿、世界回归（含钓鱼与移动射击）、Boss 18 项及视觉测试、类型检查、修改模块专项 lint 和生产构建通过。构建保留既有大分块提示；未声称全项目 lint 或模板通用测试全部通过。
